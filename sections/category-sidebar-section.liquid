{% comment %}
===================================================================
CATEGORY SIDEBAR SECTION
File: sections/category-sidebar-section.liquid

Sezione Shopify che include la sidebar delle categorie.
Da aggiungere nei template cart.json e product.json

INSTALLAZIONE:
1. Questo file è già nella cartella sections/
2. Aggiungere la sezione nei template desiderati
3. La sidebar apparirà automaticamente

UTILIZZO NEL TEMPLATE:
{
  "sections": {
    "category_sidebar": {
      "type": "category-sidebar-section",
      "settings": {}
    }
  }
}
===================================================================
{% endcomment %}

{% comment %} Include il CSS della sidebar {% endcomment %}
{{ 'category-sidebar.css' | asset_url | stylesheet_tag }}

{% comment %} Wrapper con layout grid {% endcomment %}
<div class="page-with-sidebar">

  {% comment %} Render della sidebar {% endcomment %}
  <div class="sidebar-container">
    {% render 'category-sidebar', current_collection: collection.handle %}
  </div>

  {% comment %} Contenuto principale della pagina {% endcomment %}
  <div class="main-content-with-sidebar">
    {% comment %} Il contenuto della pagina verrà inserito automaticamente {% endcomment %}
  </div>

</div>

<style>
/* Layout per pagine con sidebar */
.page-with-sidebar {
  display: flex;
  min-height: 100vh;
  position: relative;
}

.sidebar-container {
  flex-shrink: 0;
  width: 300px;
  position: relative;
}

.main-content-with-sidebar {
  flex: 1;
  margin-left: 0;
  width: 100%;
  max-width: calc(100% - 300px);
}

/* Responsive */
@media (max-width: 992px) {
  .page-with-sidebar {
    flex-direction: column;
  }

  .sidebar-container {
    width: 100%;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1000;
    height: 0;
    overflow: visible;
  }

  .main-content-with-sidebar {
    margin-left: 0;
    max-width: 100%;
    width: 100%;
  }

  /* Bottone mobile per aprire sidebar */
  .mobile-sidebar-trigger {
    position: fixed;
    bottom: 20px;
    right: 20px;
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #C12624 0%, #A01F1D 100%);
    color: white;
    border: none;
    border-radius: 50%;
    box-shadow: 0 4px 20px rgba(193, 38, 36, 0.4);
    cursor: pointer;
    z-index: 999;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    transition: all 0.3s ease;
  }

  .mobile-sidebar-trigger:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 30px rgba(193, 38, 36, 0.6);
  }

  .mobile-sidebar-trigger:active {
    transform: scale(0.95);
  }
}

@media (max-width: 576px) {
  .mobile-sidebar-trigger {
    width: 50px;
    height: 50px;
    font-size: 20px;
    bottom: 15px;
    right: 15px;
  }
}
</style>

{% comment %} Bottone mobile per aprire la sidebar {% endcomment %}
<button class="mobile-sidebar-trigger" onclick="toggleSidebar()" aria-label="Kategorien öffnen">
  ☰
</button>

<script>
// Assicurati che la funzione toggleSidebar sia disponibile
if (typeof window.toggleSidebar === 'undefined') {
  window.toggleSidebar = function() {
    var sidebar = document.getElementById('categorySidebar');
    var overlay = document.querySelector('.sidebar-overlay');
    var body = document.body;

    if (sidebar && overlay) {
      sidebar.classList.toggle('active');
      overlay.classList.toggle('active');
      body.classList.toggle('sidebar-open');
    }
  };
}

// Chiudi sidebar quando si ridimensiona la finestra a desktop
window.addEventListener('resize', function() {
  if (window.innerWidth > 992) {
    var sidebar = document.getElementById('categorySidebar');
    var overlay = document.querySelector('.sidebar-overlay');
    var body = document.body;

    if (sidebar && sidebar.classList.contains('active')) {
      sidebar.classList.remove('active');
      overlay.classList.remove('active');
      body.classList.remove('sidebar-open');
    }
  }
});
</script>

{% schema %}
{
  "name": "Category Sidebar",
  "tag": "section",
  "class": "category-sidebar-section",
  "settings": [
    {
      "type": "header",
      "content": "Sidebar Einstellungen"
    },
    {
      "type": "paragraph",
      "content": "Die Kategorie-Sidebar zeigt alle Produktkategorien an und ermöglicht eine einfache Navigation."
    }
  ],
  "presets": [
    {
      "name": "Category Sidebar"
    }
  ]
}
{% endschema %}
